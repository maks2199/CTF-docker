---
marp: true
theme: default
paginate: true
---

# Побег из докер-контейнера

## CTF Workshop

**Образовательный воркшоп по безопасности контейнеров**

---

## План воркшопа

1. Введение в Docker и контейнеризацию
2. Модель безопасности контейнеров
3. Векторы атак и техники побега
4. Практические задания (CTF)
5. Методы защиты
6. Заключение и ресурсы

---

## Введение: Что такое контейнер?

- Изолированное окружение для запуска приложений
- Использует namespaces и cgroups ядра Linux
- Не полная виртуализация - разделяет ядро с хостом
- Изоляция ≠ безопасность по умолчанию

```bash
docker run -it ubuntu:latest /bin/bash
```

---

## Модель безопасности Docker

**Уровни изоляции:**

- Namespaces (PID, NET, IPC, MNT, UTS, USER)
- Control groups (ограничение ресурсов)
- Capabilities (ограничение привилегий root)
- AppArmor/SELinux профили
- Seccomp фильтры системных вызовов

---

## Вектор #1: Privileged контейнеры

**Проблема:** `--privileged` флаг отключает большинство защит

```bash
docker run --privileged -it ubuntu /bin/bash
```

**Что получает атакующий:**

- Доступ ко всем устройствам хоста (`/dev`)
- Все Linux capabilities
- Возможность монтирования файловых систем

---

## Задание 1: Побег через privileged режим

**Цель:** Получить доступ к файловой системе хоста

**Подсказки:**

- Контейнер запущен с флагом `--privileged`
- Диски хоста доступны в `/dev`
- Используйте команды `fdisk -l` и `mount`

**Флаг находится в:** `/root/flag.txt` на хосте

---

## Вектор #2: Небезопасные монтирования

**Проблема:** Монтирование критичных директорий хоста

```bash
docker run -v /:/host -it ubuntu /bin/bash
docker run -v /var/run/docker.sock:/var/run/docker.sock
```

**Риски:**

- Прямой доступ к файлам хоста
- Возможность управления Docker API
- Запуск новых привилегированных контейнеров

---

## Задание 2: Docker socket

**Цель:** Используя примонтированный docker.sock, получите shell на хосте

**Окружение:**

- Docker socket доступен в контейнере
- Docker CLI установлен внутри контейнера

**Подсказка:** Запустите новый привилегированный контейнер

---

## Вектор #3: Kernel exploits

**Проблема:** Контейнеры используют ядро хоста

- Уязвимость в ядре = компрометация хоста
- Dirty COW (CVE-2016-5195)
- Dirty Pipe (CVE-2022-0847)
- Различные race conditions

**Пример:**

```bash
# Эксплуатация уязвимости ядра из контейнера
./exploit
```

---

## Вектор #4: Misconfigured capabilities

**Проблема:** Излишние capabilities

Опасные capabilities:

- `CAP_SYS_ADMIN` - административные операции
- `CAP_SYS_PTRACE` - отладка процессов
- `CAP_SYS_MODULE` - загрузка модулей ядра
- `CAP_DAC_READ_SEARCH` - обход проверок чтения

```bash
docker run --cap-add=SYS_ADMIN -it ubuntu
```

---

## Задание 3: CAP_SYS_ADMIN

**Цель:** Используя CAP_SYS_ADMIN, получите доступ к хосту

**Техника:**

- Создание namespace
- Монтирование файловой системы хоста
- Использование cgroups release_agent

**Подсказка:** Изучите механизм `release_agent` в cgroups

---

## Вектор #5: Exposed docker API

**Проблема:** Docker API без аутентификации

```bash
# Незащищенный Docker API на порту 2375
curl http://host:2375/containers/json

# Создание привилегированного контейнера
curl -X POST http://host:2375/containers/create \
  -H "Content-Type: application/json" \
  -d '{"Image":"ubuntu","HostConfig":{"Privileged":true}}'
```

---

## Задание 4: Remote Docker API

**Сценарий:** Найден открытый Docker API

**Цель:**

1. Перечислите запущенные контейнеры
2. Создайте привилегированный контейнер
3. Получите shell на хосте

**Инструменты:** `curl`, `docker` CLI с `-H` флагом

---

## Вектор #6: Escape через procfs

**Проблема:** Доступ к `/proc` хоста

```bash
# Если /proc не ремаппирован
cat /proc/*/environ | grep -i secret
```

**Техники:**

- Чтение environment переменных других процессов
- Доступ к файловым дескрипторам
- Использование /proc/sys для изменения параметров ядра

---

## Вектор #7: Container image vulnerabilities

**Проблема:** Уязвимости в базовых образах

- Устаревшие пакеты с известными CVE
- Внедренные backdoors
- Secrets в слоях образа

```bash
# Поиск секретов в истории образа
docker history image:tag
dive image:tag
```

---

## Задание 5: Image forensics

**Цель:** Найдите секретные данные в слоях Docker образа

**Сценарий:**

- Разработчик случайно закоммитил credentials
- Затем удалил файл в следующем слое
- Но данные остались в истории образа

**Инструменты:** `docker history`, `docker save`, `tar`

---

## Вектор #8: RunC vulnerabilities

**Примеры:**

- CVE-2019-5736: Перезапись runC бинарника
- CVE-2024-21626: Побег через file descriptor

```bash
# CVE-2019-5736 exploit
# Перезаписывает /usr/bin/runc на хосте
./exploit
```

**Защита:** Регулярные обновления Docker/runC

---

## Методы защиты: Best practices

**Рекомендации:**

✅ Никогда не используйте `--privileged` в продакшене
✅ Минимизируйте capabilities (`--cap-drop=ALL`)
✅ Используйте user namespaces (rootless mode)
✅ Применяйте AppArmor/SELinux профили
✅ Включайте seccomp фильтры
✅ Регулярно обновляйте Docker и образы
✅ Сканируйте образы на уязвимости (Trivy, Snyk)

---

## Методы защиты: Seccomp

**Ограничение системных вызовов:**

```json
{
  "defaultAction": "SCMP_ACT_ERRNO",
  "syscalls": [
    {
      "names": ["read", "write", "exit"],
      "action": "SCMP_ACT_ALLOW"
    }
  ]
}
```

```bash
docker run --security-opt seccomp=profile.json ubuntu
```

---

## Методы защиты: User namespaces

**Запуск контейнера без root:**

```bash
# Rootless Docker
dockerd-rootless-setuptool.sh install

# User namespace remapping
{
  "userns-remap": "default"
}
```

**Преимущества:**

- Root в контейнере ≠ root на хосте
- Снижение риска privilege escalation

---

## Методы защиты: Runtime security

**Мониторинг и обнаружение:**

- Falco - runtime threat detection
- Sysdig - container visibility
- Aqua Security, Prisma Cloud
- Audit logs

```yaml
# Falco rule example
- rule: Unauthorized Process
  condition: container and proc.name != expected
  output: Unexpected process in container
```

---

## Практические инструменты

**Для тестирования:**

- [docker-escape-lab](https://github.com/cdk-team/CDK)
- [bad-pods](https://github.com/BishopFox/badPods)
- [botb](https://github.com/brompwnie/botb)

**Для защиты:**

- Docker Bench Security
- Trivy scanner
- Anchore

---

## Чек-лист финального задания

**Комплексное задание:**

1. ☐ Провести разведку контейнера
2. ☐ Определить вектор атаки
3. ☐ Эксплуатировать уязвимость
4. ☐ Получить доступ к хосту
5. ☐ Найти финальный флаг
6. ☐ (Бонус) Предложить меры защиты

---

## Ресурсы для изучения

**Документация:**

- [Docker Security](https://docs.docker.com/engine/security/)
- [CIS Docker Benchmark](https://www.cisecurity.org/benchmark/docker)

**Статьи и исследования:**

- "Understanding Docker Container Escapes"
- OWASP Docker Security Cheat Sheet

**CTF платформы:**

- HackTheBox (Docker challenges)
- TryHackMe (Container Security)

---

## Вопросы и обсуждение

**Контакты:**

- Вопросы по заданиям
- Обсуждение техник
- Feedback

**Помните:**
Все техники только для легального тестирования
в контролируемых окружениях! 🔒

---

## Спасибо за участие! 🎯

**Следующие шаги:**

- Попробуйте запустить собственные CTF задания
- Проверьте свою инфраструктуру
- Делитесь знаниями с командой

**Stay secure!** 🛡️
